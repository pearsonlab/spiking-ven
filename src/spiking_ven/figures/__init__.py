"""Publication figures.

Requires the ``plots`` extra (matplotlib). ``compute_encoding_columns`` in
:mod:`~spiking_ven.figures.encoding_comparison` is the single definition of the VEN
inference used by every view here -- do not reimplement it.
"""
